# Go binaries (built at repo root without -o flag)
bin/
/agent
/consumer
/api

# Go build cache
*.test
*.out

# Log files from debugging sessions
*.log

# Dependency vendor dir (if ever added)
vendor/
